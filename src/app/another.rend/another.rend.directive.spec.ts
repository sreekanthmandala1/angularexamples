import { AnotherRendDirective } from './anotherrend.directive';

describe('Another.RendDirective', () => {
  it('should create an instance', () => {
    const directive = new AnotherRendDirective();
    expect(directive).toBeTruthy();
  });
});
