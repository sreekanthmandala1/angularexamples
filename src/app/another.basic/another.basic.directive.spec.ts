import { Another.BasicDirective } from './another.basic.directive';

describe('Another.BasicDirective', () => {
  it('should create an instance', () => {
    const directive = new Another.BasicDirective();
    expect(directive).toBeTruthy();
  });
});
