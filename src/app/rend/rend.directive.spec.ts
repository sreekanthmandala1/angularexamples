import { RendDirective } from './rend.directive';

describe('RendDirective', () => {
  it('should create an instance', () => {
    const directive = new RendDirective();
    expect(directive).toBeTruthy();
  });
});
